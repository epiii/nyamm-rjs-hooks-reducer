const contact = (state, action) => {
    const { type } = action
    switch (type) {
        // case value:

        //     break;

        default:
            return state
            break;
    }
}

export default contact